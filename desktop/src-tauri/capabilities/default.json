{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main and ask windows",
  "windows": ["main", "ask"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
